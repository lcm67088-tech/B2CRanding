# CHANGELOG

## 2026-05-29

- `origin` 원격 저장소가 `https://github.com/lcm67088-tech/B2CRanding.git`로 연결되어 있음을 확인했다.
- 기존 `index.html`, `styles.css`, `script.js`를 `archives/2026-05-29-before-v2/`에 보관했다.
- `index-v2.html`, `styles-v2.css`, `script-v2.js` 내용을 기본 배포 파일 `index.html`, `styles.css`, `script.js`에 반영했다.
- `index.html`의 스타일과 스크립트 참조를 `./styles.css`, `./script.js`로 정리했다.
- 프로젝트 문서 `AGENTS.md`, `PROJECT_NOTES.md`, `CHANGELOG.md`, `ERROR_LOG.md`, `README.md`를 정리했다.
- 설문 응답 저장용 `Google Apps Script`(구글 앱스 스크립트: 구글 서비스 자동화 코드)를 실제 시트 컬럼명 기준 매핑 방식으로 수정했다.
- `script-v2.js`와 `script.js`의 폼 전송 payload가 현재 폼 입력값만 보내도록 정리했다.
- `script-v2.js`와 `script.js`의 상담 폼 전송 URL을 새 `Google Apps Script` 웹 앱 배포 URL로 교체했다.
